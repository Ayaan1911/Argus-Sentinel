
# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Node
node_modules/
.npm

# Build artifacts
dist/
build/
*.zip

# Python cache
*.pyc

# Celery beat's local schedule state (shelve DB) — written into the
# bind-mounted backend/ dir by the worker's embedded beat (-B flag in
# docker-compose.yml), not a source file.
celerybeat-schedule



notes.md
docker-compose.override.yml